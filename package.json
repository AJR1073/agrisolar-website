{
  "name": "agrisolar-website",
  "version": "1.0.0",
  "description": "AgriSolar LLC Website",
  "scripts": {
    "build": "mkdir -p y && cp -r css js images gallery index.html form-submitted.html y/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase": "^9.23.0"
  }
}
